//page 5
//Option-click the two arrays below and look at their types
let grades = ["A", "B", "C", "D", "E"] // String
let starRatings = [1, 2, 3, 4, 5] // Int
//What is the type of grades? What is the type of starRatings?
let someGrade = grades[0] // String
let someRating = starRatings[0] // Int
Try setting anotherGrade to a number. What happens?
var anotherGrade = grades[1] // B 
var anotherGrade = grades[3] // D
//page 13
//Using what you’ve learned about making decisions, write an if statement that will only access the array if the value of index is less than the array's count. Uncomment and update the code below, then update the index constant to different numbers and see the results.
let index = 3
if index < devices.count {
    devices[index]
}

// page 14
// Create an empty array to hold song titles as strings, and use the append method to add three or four songs one at a time.
var songList: [String] = []
songList.append("Mascara")
songList.append("Undone")
songList.append("No Surpises")
songList.append("Stone")
//One enthusiastic singer wants to add three songs at once. Create an array holding this one singer's song list and use the += operator to append their whole list to the end of the group's song list.
let enthusiasticSingerSongs = [
    "Spins",
    "Shake it off",
    "Frontin"
]
songList += enthusiasticSingerSongs
//Write a for…in loop and, for every song title in the array, print an encouraging announcement to let the next singer know that it's their turn.
for song in songList {
    print ("Now singing: /(song)! Get ready!)")
}
//After the loop has called everyone up to sing, use the removeAll method on the song list to clear out all the past songs.
songList.removeAll()

//page 15
//Create two variables, one to count yes votes and one to count no votes. Each should start off with a value of zero.
callout(Exercise): Create a for…in loop that iterates over one of the vote arrays and checks the value of each vote. If the vote is true, the loop should add one vote to the yes variable. If it's false, it should add one vote to the no variable.
var yesshouldMascotChangeVotes = 0
var noshouldMascotChangeVotes = 0

for shouldMascotChangeVotes in shouldMascotChangeVotes {
    if shouldMascotChangeVotes == true {
        yesshouldMascotChangeVotes += 1
    } else {
        noshouldMascotChangeVotes += 1
    }
}

var yesshouldInstallCoffeeVendingMachineVotes = 0
var noshouldInstallCoffeeVendingMachineVotes = 0

for shouldInstallCoffeeVendingMachineVotes in shouldInstallCoffeeVendingMachineVotes {
    if shouldInstallCoffeeVendingMachineVotes == true {
        yesshouldInstallCoffeeVendingMachineVotes += 1
    } else {
        noshouldInstallCoffeeVendingMachineVotes += 1
    }
}

var yesshouldHaveMorePollOptionsVotes = 0
var noshouldHaveMorePollOptionsVotes = 0

for shouldHaveMorePollOptionsVotes in shouldHaveMorePollOptionsVotes {
    if shouldHaveMorePollOptionsVotes == true {
        yesshouldHaveMorePollOptionsVotes += 1
    } else {
        noshouldHaveMorePollOptionsVotes += 1
    }
}
//After the loop has finished, write an if statement that compares the two values and prints a different message based on whether the vote passed or failed.
if yesshouldMascotChangeVotes > noshouldMascotChangeVotes {
    print ("The Vote passed")
} else {
    print ("The Vote failed")
}

if yesshouldHaveMorePollOptionsVotes > noshouldHaveMorePollOptionsVotes {
    print ("The Vote passed")
} else {
    print ("The Vote failed")
}

if yesshouldInstallCoffeeVendingMachineVotes > noshouldInstallCoffeeVendingMachineVotes {
    print ("The Vote passed")
} else {
    print ("The Vote failed")
}
//Write a function that takes two arguments: a string describing the issue being voted on and an array with the issue's Bool votes. Move the for…in loop and the if statement inside the function, so it prints the results when you call the function with a particular issue's arguments. You should be able to call the function like this:
func printResults (forIssue issue: String, withVotes votes: [Bool]) {
    var yesVotes = 0
    var noVotes = 0
    for vote in votes {
        if vote {
            yesVotes += 1
        } else {
            noVotes += 1
        }
    }
    print ("\(issue) \(yesVotes) yes, \(noVotes) no")
}
printResults(forIssue: "Should we change the mascot?", withVotes: shouldMascotChangeVotes)

//page 16
//Create an array literal with 20 to 25 items of sample data for your daily activity. It may be something like let milesBiked = [3, 7.5, 0, 0, 17 ... ] Feel free to make up or embellish the numbers, but make sure you have entries that are above, below and exactly at the goal you've thought of. Hint: Make sure to choose the right kind of array for your data, whether [Double] or [Int].
let dailyExerciseMinutes: [Int] = [
  30, 45, 60, 70, 55,
  60, 80, 40, 25, 60,
  90, 50, 60, 35, 65,
  75, 20, 60, 85, 55
]
//Write a function that takes the daily number as an argument and returns a message as a string. It should return a different message based on how close the number comes to your goal. You can be as ambitious and creative as you'd like with your responses, but make sure to return at least two different messages depending on your daily progress!
func exerciseFeedback(minutes: Int) -> String {
    let goal = 60
    if minutes == goal {
        return "You hit your goal. Well done!"
    } else if minutes > goal {
        "Great job! You exceeded your goal!"
    }
    return "Keep going! You were below your goal today."

}
//Write a for…in loop that iterates over your sample data, calls your function to get an appropriate message for each item, and prints the message to the console.
for minutes in dailyExerciseMinutes {
    let message  = exerciseFeedback(minutes: minutes)
    print (message)
}
